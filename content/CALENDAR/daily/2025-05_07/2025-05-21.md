---
link: "[[_Daily Notes]]"
---
### spustit cv pipeline na 211

uninstalled `sensor-manager` package
```bash
export GOOGLE_APPLICATION_CREDENTIALS="/data/keys/service_account.json"
PROMETHEUS_MULTIPROC_DIR="$(mktemp -d)" CLOUDPATHLIB_FILE_CACHE_MODE=cloudpath_object /data/git/cv_pipeline/bin/sensor_manager.py --mqtt-config /data/configs/sensor-manager/mqtt_options.json /data/configs/sensor-manager/sensor_manager_config.json
```

pro pripad ze uninstall `sensor-manager` odstrani `sensor_manager.service` file:
```bash

[Unit]
Description=Sensor_Manager
After=network.target

[Service]
Environment=GOOGLE_APPLICATION_CREDENTIALS=/data/keys/service_account.json
Type=simple
Restart=always
User=rs-ops
ExecStart=/bin/bash -c 'PROMETHEUS_MULTIPROC_DIR="$(mktemp -d)" CLOUDPATHLIB_FILE_CACHE_MODE=cloudpath_object /usr/bin/sensor_manager.py --mqtt-config /data/configs/sensor-manager/mqtt_options.json /data/configs/sensor-manager/sensor_manager_config.json'

[Install]
WantedBy=network-online.target
```

#TODO: pak znovu nainstalluj `sensor-manager`


```mermaid
graph TD
    subgraph "Frontend (User Interface)"
        A[User Browser/App] --> B[React/Angular/Vue.js]
        B -- Serves Static Assets --> C[Firebase Hosting]
    end

    subgraph Authentication
        B -- Authenticates with --> D[Firebase Authentication]
        D -- Issues ID Token --> B
    end

    subgraph "Backend API (Google Cloud Run)"
        E["Main API Service (Python/FastAPI/Flask)"]
        B -- Authorized HTTP Request --> E
        E -- Stores Auth State/User Data --> F["Cloud Firestore/Cloud SQL"]
        E -- Initiates File Upload --> G["Cloud Storage (Signed URL)"]
        G -- Uploads Raw Data Directly --> B
    end

    subgraph "Asynchronous Processing (Google Cloud)"
        E -- Publishes Message --> H["Cloud Pub/Sub Topic"]
        H -- Triggers --> I["Heavy Processing Worker (Google Cloud Run)"]
        I -- Accesses Raw Data --> J["Cloud Storage (Raw Data Bucket)"]
        I -- Performs Computation --> K["Compute Engine Instance (Optional for extreme cases)"]
        K -- Stores Results --> L["Cloud Storage (Processed Data Bucket)"]
        I -- Updates Status/Metadata --> F
        I -- Sends Notification (Optional) --> M["Cloud Functions/Cloud Run (for Notifications)"]
    end

    subgraph "Data Storage"
        F
        J
        L
    end

    style A fill:#f9f,stroke:#333,stroke-width:2px
    style B fill:#9ff,stroke:#333,stroke-width:2px
    style C fill:#f9f,stroke:#333,stroke-width:2px
    style D fill:#f9f,stroke:#333,stroke-width:2px
    style E fill:#ccf,stroke:#333,stroke-width:2px
    style F fill:#cfc,stroke:#333,stroke-width:2px
    style G fill:#ccf,stroke:#333,stroke-width:2px
    style H fill:#fcc,stroke:#333,stroke-width:2px
    style I fill:#ccf,stroke:#333,stroke-width:2px
    style J fill:#cfc,stroke:#333,stroke-width:2px
    style K fill:#ffc,stroke:#333,stroke-width:2px
    style L fill:#cfc,stroke:#333,stroke-width:2px
    style M fill:#fcc,stroke:#333,stroke-width:2px
```


